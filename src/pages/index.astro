---
import BaseLayout from '../layouts/BaseLayout.astro';
import { env } from 'cloudflare:workers';
import { getHomepageSettings, getDefaults } from '../lib/homepage-db';
import DonateWidget from '../components/book/DonateWidget.astro';
import { getSupportSettings } from '../lib/book-db';

export const prerender = false;

const homepage = await getHomepageSettings(env.DB) ?? getDefaults();

const supportSettings = (await getSupportSettings(env.DB)) ?? {
  enabled: 0,
  title: 'Ủng Hộ Dental Empire OS',
  message: '',
  qr_url: '/images/qr/donation-qr.svg',
  payment_methods: '',
};

const tiers = [
  { tier: 1, name: homepage.tier1_name, subtitle: homepage.tier1_subtitle, icon: homepage.tier1_icon },
  { tier: 2, name: homepage.tier2_name, subtitle: homepage.tier2_subtitle, icon: homepage.tier2_icon },
  { tier: 3, name: homepage.tier3_name, subtitle: homepage.tier3_subtitle, icon: homepage.tier3_icon },
];

const testimonialsData = [
  {
    quote: homepage.testimonial1_quote,
    name: homepage.testimonial1_name,
    role: homepage.testimonial1_role,
    avatar: homepage.testimonial1_avatar,
  },
  {
    quote: homepage.testimonial2_quote,
    name: homepage.testimonial2_name,
    role: homepage.testimonial2_role,
    avatar: homepage.testimonial2_avatar,
  },
  {
    quote: homepage.testimonial3_quote,
    name: homepage.testimonial3_name,
    role: homepage.testimonial3_role,
    avatar: homepage.testimonial3_avatar,
  },
];
---

<BaseLayout title="Dental Empire OS - Hệ Điều Hành Quản Trị Nha Khoa Toàn Diện">

  <!-- Hero Section -->
  <section class="relative min-h-[560px] flex items-center overflow-hidden">
    <div class="absolute inset-0 z-0">
      <div class="absolute inset-0 bg-gradient-to-r from-surface-container-lowest via-surface-container-lowest/90 to-transparent"></div>
    </div>
    <div class="relative z-10 w-full px-6 max-w-[1200px] mx-auto grid md:grid-cols-2 gap-10 items-center py-12">
      <div class="space-y-5">
        <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-primary-container/20 border border-primary/30 text-primary">
          <span class="material-symbols-outlined text-[18px]">verified</span>
          <span class="font-semibold text-sm tracking-wider">{homepage.badge_text}</span>
        </div>
        <h1 class="text-headline-xl leading-tight text-white">
          {homepage.hero_title_line1.split(':')[0]}:<br />
          <span class="text-primary">{homepage.hero_title_line1.split(':').slice(1).join(':').trim()}</span> {homepage.hero_title_line2}
        </h1>
        <p class="text-on-surface-variant max-w-lg leading-relaxed">{homepage.hero_description}</p>
        <div class="flex flex-wrap gap-4 pt-2">
          <a href="/book" class="px-7 py-3 btn-primary-metallic font-bold rounded-xl hover:-translate-y-1 active:scale-95 transition-all">{homepage.hero_cta_primary}</a>
          <a href="/about" class="px-7 py-3 border-2 border-outline text-on-surface-variant font-bold rounded-xl hover:border-primary hover:text-primary active:scale-95 transition-all">{homepage.hero_cta_secondary}</a>
        </div>
        <div class="flex items-center gap-6 pt-4 border-t border-outline-variant">
          <p class="text-on-surface-variant font-semibold">{homepage.hero_stats_text}</p>
        </div>
      </div>
      <div class="hidden md:block relative">
        <div class="absolute inset-0 bg-primary-container/10 rounded-[2rem] -rotate-3 blur-2xl"></div>
        <img
          src={homepage.hero_image_url}
          alt="Dental Empire OS - Bìa sách"
          class="relative z-10 w-full rounded-[2rem] shadow-2xl border-4 border-surface-container-high object-cover"
          loading="eager"
        />
      </div>
    </div>
  </section>

  <!-- Core Modules (Bento Grid) -->
  <section class="py-20 bg-surface-container-low">
    <div class="px-6 max-w-[1200px] mx-auto">
      <div class="text-center mb-16 space-y-4">
        <h2 class="text-headline-lg text-white">{homepage.tier_section_heading}</h2>
        <p class="text-on-surface-variant max-w-2xl mx-auto">{homepage.tier_section_subheading}</p>
      </div>
      <div class="flex flex-col gap-8 max-w-4xl mx-auto">
        {tiers.map((mod, index) => (
          <div class:list={[
            'group relative p-8 rounded-[2rem] border transition-all duration-500',
            index === 0
              ? 'bg-surface-container border-primary/30 hover:border-primary shadow-[0_0_30px_rgba(146,204,255,0.1)]'
              : 'bg-surface-container border-outline-variant hover:border-primary/50',
          ]}>
            <div class:list={[
              'absolute top-0 right-0 p-6 transition-opacity',
              index === 0 ? 'opacity-20 group-hover:opacity-100' : 'opacity-10 group-hover:opacity-50',
            ]}>
              <span class="material-symbols-outlined text-6xl text-primary">{mod.icon}</span>
            </div>
            <div class="relative z-10">
              <div class:list={[
                'inline-block px-4 py-1 rounded-full font-semibold text-sm tracking-wider mb-4',
                index === 0 ? 'bg-primary/10 text-primary' : 'bg-white/5 text-on-surface-variant',
              ]}>
                TẦNG {mod.tier}
              </div>
              <h3 class="text-headline-md text-white mb-4">{mod.name}</h3>
              <p class="text-on-surface-variant leading-relaxed max-w-2xl">
                {mod.subtitle}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Social Proof / Testimonials -->
  <section class="py-20 overflow-hidden bg-surface">
    <div class="px-6 max-w-[1200px] mx-auto">
      <div class="flex flex-col md:flex-row justify-between items-end gap-6 mb-16">
        <div class="max-w-xl">
          <h2 class="text-headline-lg text-white mb-4">{homepage.testimonial_section_heading}</h2>
          <p class="text-on-surface-variant">{homepage.testimonial_section_subheading}</p>
        </div>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {testimonialsData.map((t) => (
          <div class="bg-surface-container-low p-8 rounded-2xl border border-outline-variant shadow-sm relative">
            <span class="absolute top-6 right-8 text-6xl text-primary/10">&ldquo;</span>
            <div class="flex items-center gap-1 text-primary mb-4">
              {[1, 2, 3, 4, 5].map(() => (
                <span class="material-symbols-outlined" style="font-variation-settings: 'FILL' 1;">star</span>
              ))}
            </div>
            <p class="text-on-surface-variant mb-8 italic leading-relaxed">
              &ldquo;{t.quote}&rdquo;
            </p>
            <div class="flex items-center gap-4">
              <img
                class="w-12 h-12 rounded-full object-cover"
                src={t.avatar}
                alt={t.name}
                loading="lazy"
              />
              <div>
                <p class="font-bold text-white">{t.name}</p>
                <p class="text-xs text-on-surface-variant">{t.role}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Authority / Founder Section -->
  <section class="py-20 bg-surface-container-lowest text-on-surface overflow-hidden">
    <div class="px-6 max-w-[1200px] mx-auto grid md:grid-cols-2 gap-16 items-center">
      <div class="relative group">
        <div class="absolute -inset-4 bg-primary-container/10 rounded-3xl blur-3xl opacity-0 group-hover:opacity-100 transition-all duration-500"></div>
        <div class="relative z-10 w-full aspect-[3/4] rounded-3xl shadow-2xl border-2 border-outline-variant bg-gradient-to-br from-surface-container to-surface-container-high flex items-center justify-center overflow-hidden">
          {homepage.founder_image_url ? (
            <img
              src={homepage.founder_image_url}
              alt={homepage.founder_name}
              class="w-full h-full object-cover rounded-3xl"
              onerror="this.parentElement.innerHTML=`<div class='text-center space-y-4 p-8'><span class='material-symbols-outlined text-9xl text-primary/30'>person</span><div class='text-headline-md font-bold text-white'>${this.alt}</div><div class='text-on-surface-variant text-sm'>${this.dataset.title || ''}</div></div>`"
              data-title={homepage.founder_title}
            />
          ) : (
            <div class="text-center space-y-4 p-8">
              <span class="material-symbols-outlined text-9xl text-primary/30">person</span>
              <div class="text-headline-md font-bold text-white">{homepage.founder_name}</div>
              <div class="text-on-surface-variant text-sm">{homepage.founder_title}</div>
            </div>
          )}
        </div>
      </div>
      <div class="space-y-8">
        <div class="inline-block px-4 py-1 rounded-full border border-outline bg-white/5 font-semibold text-sm tracking-wider">{homepage.founder_badge}</div>
        <h2 class="text-headline-lg text-white">{homepage.founder_heading}</h2>
        <p class="text-on-surface-variant leading-relaxed">{homepage.founder_bio}</p>
        <div class="grid grid-cols-2 gap-8 py-4">
          <div>
            <div class="text-4xl font-bold text-primary mb-2">{homepage.founder_stat1_value}</div>
            <div class="text-sm text-on-surface-variant">{homepage.founder_stat1_label}</div>
          </div>
          <div>
            <div class="text-4xl font-bold text-primary mb-2">{homepage.founder_stat2_value}</div>
            <div class="text-sm text-on-surface-variant">{homepage.founder_stat2_label}</div>
          </div>
        </div>
        <a class="inline-flex items-center gap-2 group font-bold text-primary hover:text-white transition-colors" href="/about">
          {homepage.founder_link_text}
          <span class="material-symbols-outlined group-hover:translate-x-2 transition-transform">arrow_forward</span>
        </a>
      </div>
    </div>
  </section>

  <!-- Secondary CTA Section -->
  <section class="py-20 bg-surface">
    <div class="px-6 max-w-[1200px] mx-auto">
      <div class="bg-surface-container-low border border-outline-variant rounded-[3rem] p-12 md:p-20 relative overflow-hidden text-center">
        <div class="absolute top-0 right-0 w-64 h-64 bg-primary/5 rounded-full -translate-y-1/2 translate-x-1/2 blur-3xl"></div>
        <div class="absolute bottom-0 left-0 w-64 h-64 bg-primary/5 rounded-full translate-y-1/2 -translate-x-1/2 blur-3xl"></div>
        <div class="relative z-10 max-w-3xl mx-auto space-y-8">
          <h2 class="text-headline-xl text-white">{homepage.cta_heading}</h2>
          <p class="text-on-surface-variant">{homepage.cta_subtext}</p>
          <div class="flex flex-col sm:flex-row gap-6 justify-center pt-4">
            <a href={homepage.cta_primary_link || '#'} class="px-10 py-5 btn-primary-metallic font-bold rounded-2xl shadow-lg active:scale-95 transition-all flex items-center justify-center gap-3">
              <span class="material-symbols-outlined">{homepage.cta_primary_icon}</span>
              {homepage.cta_primary_text}
            </a>
            <a href={homepage.cta_secondary_link || '/resources'} class="px-10 py-5 border-2 border-outline text-on-surface-variant font-bold rounded-2xl hover:border-primary hover:text-primary shadow-lg active:scale-95 transition-all flex items-center justify-center gap-3">
              <span class="material-symbols-outlined">{homepage.cta_secondary_icon}</span>
              {homepage.cta_secondary_text}
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Compact Donate Widget -->
  <section class="py-12 bg-transparent">
    <div class="px-6 max-w-[1200px] mx-auto">
      <DonateWidget settings={supportSettings} />
    </div>
  </section>

</BaseLayout>
